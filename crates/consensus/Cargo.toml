[package]
name = "consensus"
description = "Burn-to-mine consensus client: Zcash follower, burn parser, sealer, gossip"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
thiserror.workspace = true
serde_json.workspace = true
hex.workspace = true
ureq.workspace = true
# Only used by `src/bin/consensus_sim.rs` (the C6 simulation harness's
# STREAM_DIGEST) — the library itself has no hashing needs.
sha2 = "0.10"
