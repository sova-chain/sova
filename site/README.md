# sova.io

The Sova website: a vision landing page at `/` (Sova is the EVM that can see
Zcash), the whitepaper at `/paper` (`src/components/Paper.astro`, also at
`/v/paper`), and plain pages beside them: `/why`, `/build`, `/sips`, `/mine`,
`/node`, `/ecosystem`, `/story`, `/ashwings` and `/press`. It's built with
[Astro](https://astro.build) as a fully static site. The paper uses one small
vanilla script (the table of contents' active-section highlight); the landing
page's terminal is CSS-only, and its one inline line of script forwards old
paper links (`/#s4`) to `/paper#s4`; the other pages use no script.
Pre-release.

## Commands

Requires Node 22.12 or newer (Astro 7's floor; see `.nvmrc`).

```bash
cd site
npm ci            # install exactly what package-lock.json pins
npm run dev       # dev server with hot reload, http://localhost:4321
npm run build     # static build into site/dist/
npm run preview   # serve site/dist/ locally to check the real build
```

Astro's anonymous telemetry is switched off in every script
(`ASTRO_TELEMETRY_DISABLED=1`), so building doesn't contact anything.

## Layout

| Path | What |
| --- | --- |
| `src/pages/index.astro` | The homepage: direction C, "Cover" (Rob's pick, 2026-09-23), the same page as `/v/cover` but indexable and without the direction switcher. A paper's cover sheet in the paper's Computer Modern subset: title "The EVM that reads Zcash.", a two-sentence abstract, Figure 1 (the two chains, drawn with CSS), Table 1 (testnet stats placeholder), and the links as a table of contents (`coverToc` in `src/data/sova.ts`, Rob's order of 2026-09-23: Why, Mine, Run a node, Build, Ecosystem, GitHub, Contributing, Whitepaper, SIPs; the footer carries Telegram and X). Its claims are sourced in the `/v/cover` rows below; the previous landing page is in git history |
| `src/pages/paper.astro` | Renders the paper (`src/components/Paper.astro`), indexable |
| `src/pages/{build,sips}.astro` | `/build`: the SIP-4 precompile for developers, plus how to contribute (`#contribute`; merged here instead of a separate `/contribute`). `/sips`: every SIP with status, how a SIP moves, how to propose one |
| `src/components/Tag.astro` | Status tags (`[local]`, `[testnet]`, `[planned]`, `[at launch]`, and the SIP statuses), styled in `Site.astro` |
| `src/components/Paper.astro` | The whitepaper, v5 (`docs/paper/sova-paper-v5.md`, set as written; section map, figure specs and sources in `docs/paper/v5-notes.md`): eleven Letter sheets, sections 1-11, equations (1)-(3), Figures 1-3 drawn as inline SVG line art in the paper's ink (no motion), references [1]-[10]. Section ids are names: `#intro`, `#burns`, `#epochs`, `#issuance`, `#sealing`, `#network`, `#verification`, `#reading-zcash`, `#privacy`, `#calculations`, `#conclusion`, `#refs`; figures `#fig1`-`#fig3`. The v4 ids stay as empty spans at the equivalent place so old links land: `#s1` intro, `#s2` burns, `#s3` epochs, `#s3-3` epochs (fork choice paragraph), `#s4` issuance, `#s3-2` sealing, `#s3-1` sealing (withdrawals paragraph), `#s6` network, `#s5`/`#s5-1` verification, `#s5-2` verification (held blocks paragraph), `#s-zcash` reading Zcash, `#s7` privacy, `#fig4` the worked epoch in §10, and `#s8`/`#s9`/`#s10` (cut in v5) the conclusion. A sticky table of contents sits in the left margin at 1200 px and wider (IntersectionObserver marks the current section with `aria-current`); narrower screens get a "Contents" disclosure on the first sheet. A row under the title block, the TOC and the footer link the pages below. Every rendered character, figure text included, stays inside the font subset (printable ASCII plus `· × Σ — ’ “ ” … → −` and no-break space) |
| `src/layouts/Site.astro` | Shared layout for every page but the paper: /v/terminal's gold-on-black look, calmed (faint scanlines, no script), one nav of nine items in the homepage's order (why, mine, node, build, ecosystem, ashwings, story, whitepaper, sips; `/press` is footer-only), indexable |
| `src/pages/{why,story,mine,node,ashwings,press}.astro` | The pages beside the paper (sources in the claim table below) |
| `src/pages/ecosystem.astro` | `/ecosystem`: what's built on Sova, in three groups kept apart: the network itself, apps built on it (Ashwings, Zcash pulse, the day-one kit), and products Sova Labs operates (wZcash, custodial, at wz.cash, marked soon). Ends with "build yours". In the nav, footers and sitemap |
| `src/pages/ashwings/buy.astro`, `src/scripts/checkout/` | Unlisted demo: buy an Ashwing with ZEC (noindex, not in `sitePages`). Vanilla client JS: JSON-RPC, ABI, QR. Runbook: `docs/design/ashwing-zec-checkout.md` |
| `src/pages/ashwings/{mint,market}.astro`, `src/scripts/ashwings/`, `src/styles/owls.css` | Unlisted until launch (noindex, not in `sitePages`): mint for SOVA (injected wallet) or hand off to `/ashwings/buy` for ZEC, a gallery of minted owls (each card is the contract's `tokenURI`); the market (list, buy, cancel for SOVA). Config `?rpc=&ashw=&market=&relayer=`, defaults the public testnet (`SOVA_RPC`, addresses imported from `infra/testnet/deployments/sova-testnet.json`); the local box after `box/deploy-dapps.sh` by override. Connecting a wallet switches it to the RPC's chain, adding it first if the wallet doesn't know it (chain 82330 as "Sova testnet", the public RPC, currency SOVA). Vanilla client JS, batched JSON-RPC, reuses the checkout helpers. A sent transaction shows a waiting state (`waitingForBlock` in `src/scripts/checkout/chain.ts`, also used by `/ashwings/buy`): "sent, waiting for a block", an elapsed-time clock (aria-hidden, so screen readers hear the status once), and one line, "Sova makes one block per Zcash block: about a minute, sometimes several." (`docs/design/faster-blocks.md` §1, §4 item 1); receipts are awaited up to 10 min. e2e: `tools/checkout-relayer` flows D and E |
| `src/pages/pulse.astro`, `src/scripts/pulse/` | Unlisted demo "Zcash pulse" (SIP-7, noindex, not in `sitePages`): live Zcash value pools read from the `ZcashBlocks` system contract (`0x…5A01`) over Sova RPC (`?rpc=`, `?at=`, `?n=`). Vanilla client JS, reuses the checkout's JSON-RPC/ABI helpers. Local e2e: `tools/pulse-feed/e2e.mjs` |
| `src/pages/sitemap.xml.ts`, `public/robots.txt` | Sitemap of the indexable pages (`/` plus every `sitePages` entry); `/v/*`, `/ashwings/{buy,mint,market}` and `/pulse` stay out and noindex |
| `public/brand/` | Logo files for `/press`, copied from `brand/` (SVG and PNG only, about 130 KB): wordmark in gold, deep gold, white and black; the round and square mark in gold-on-black and deep-gold-on-white; the inverted mark as PNG. The 5 MB banner JPG is left out |
| `src/layouts/Base.astro` | `<head>`, font preloads, skip link. Future docs pages reuse it |
| `src/styles/global.css` | Brand tokens (from `brand/readme.md`) and all styles |
| `public/fonts/` | Self-hosted Newsreader (variable, opsz + wght) and Roboto Mono (variable), Latin subset only, with OFL licenses. `paper-serif-{roman,italic,bold}.woff2` (about 29 KB together) are CMU Serif (Computer Modern) subset to printable ASCII plus the few symbols `/v/paper` uses, renamed "Sova Paper Serif" per the OFL's reserved-name clause (`LICENSE-CMU-Serif.txt`). Re-subset if that page gains new non-ASCII characters |
| `public/ashwings/` | All twelve owls from `contracts/samples/` (`onchain-0..11.svg`), rendered by the Ashwings contract |
| `public/favicon.svg` | `brand/SOVA PFP/SOVA_PFP_COLOR.svg` |
| `src/pages/v/{2009,terminal,burn,paper}.astro` | Candidate redesigns (pre-decision), at `/v/2009`, `/v/terminal`, `/v/burn`, and `/v/paper` (2009 refined into a typeset paper whose figures are terminal panels); `/v` lists them. Unlike the index they use small vanilla scripts for motion, all honouring `prefers-reduced-motion` |
| `src/pages/v/{block,fire,cover}.astro` | The 2026-09-23 homepage round (`docs/marketing/homepage-directions.md`), three complete homepages at `/v/block` (a terminal mines a block; the two chains grow one for one), `/v/fire` (a ZEC coin burns, a SOVA coin rises) and `/v/cover` (a paper's cover sheet; the figure draws itself; links as a table of contents). Noindex, out of the sitemap. No script: all motion is CSS (per-line keyframes generated at build time on `/v/block`), and reduced motion shows the finished frame. Shared doors and links: `doors`, `readLinks` in `src/data/sova.ts` |
| `src/components/TestnetStats.astro`, `src/scripts/testnet-stats.ts` | The live-testnet slot on the homepage directions: Sova height, anchored Zcash height, age of the last block. Filled in the browser from `SOVA_RPC` (`?rpc=` overrides), two calls per page view, no polling: `eth_getBlockByNumber('latest')` and `ZcashBlocks.latest()` (SIP-7, shared with `/pulse` via `src/scripts/zcash-blocks.ts`). The `------` placeholders stay with no JS or when the RPC can't be read. No burns or SOVA-minted totals: no RPC exposes them |
| `src/layouts/Direction.astro`, `src/components/DirSwitch.astro` | Head and cross-links for the `/v/*` pages (no shared stylesheet); DirSwitch lists all seven directions |
| `src/data/sova.ts`, `src/data/owl-ascii.ts` | Shared facts (the page list, the SIP list with each SIP's file, the repository URLs and footer links, positioning lines; sourced per the table below), and the build-time Ashwings-to-ASCII renderer |

No third-party requests: fonts, images and CSS are all first-party. The CSS is
inlined into the HTML at build time.

## Deploying later on Cloudflare Pages (not set up)

| Setting | Value |
| --- | --- |
| Framework preset | Astro |
| Root directory | `site` |
| Build command | `npm run build` |
| Build output directory | `dist` |
| Environment variable | `NODE_VERSION=22` (or newer) |

The source is public at <https://github.com/sova-chain/sova> (default branch
`main`, an export of the `release` trunk with the same file layout), so every
source, spec, contributing and security link points there. What still has no
URL stays a non-link tagged `data-placeholder="…"`; `grep -rn data-placeholder
src/` finds every one.

## Links to the public repository

Every URL is built from `repo` / `CLONE` in `src/data/sova.ts`; SIP links use
`sips[].file`, the exact file name in `sips/` (SIP-5 has no file yet, so it
stays unlinked and `[planned]`). If a SIP file is renamed (for example when a
draft drops `-draft-` from its name), update `file` there.

| Where | Link |
| --- | --- |
| Footer of every `Site.astro` page, the paper, `/` and `/v/*` (`footerLinks`) | "Source" → the repo; "Specs" → `tree/main/sips`; "Docs" → `tree/main/docs`; "Telegram" → <https://t.me/sovazec>; "X" → <https://x.com/sovazec> (`SOCIAL`) |
| `/press` "community" | Telegram and X (`SOCIAL`) |
| `/` "the SIPs" list | Each SIP → `blob/main/sips/<file>` (SIP-5 unlinked); a line to `/sips` |
| `/` "open source" | `git clone https://github.com/sova-chain/sova && cd sova`, the URL linked |
| `/sips` | Each SIP title → its file; "the texts" → `tree/main/sips`; "propose one" → Discussions (the root: a SIPs category may not exist yet) |
| `/build#try` | Clone command first in the block |
| `/build` source line | `contracts/src/zcash/` → `tree/main/contracts/src/zcash`; `sips/sip-4` → `blob/main/sips/sip-4-draft-zcash-state-precompile.md` |
| `/build#contribute` | The repo, `CONTRIBUTING.md`, Discussions, Issues, private vulnerability reporting (`security/advisories/new`, needs a GitHub login) and `SECURITY.md` |
| `/node` "start" block | `git clone https://github.com/sova-chain/sova && cd sova`, then `./box/up.sh` |
| Paper references [2]-[5], [9], [10] | Each SIP file (`sips[].file`; SIP-7 is not in `sips` yet, so `Paper.astro` names `sip-7-draft-zcash-events.md`), URL shown in full like [1] and [6]-[8] (printable ASCII, inside the paper's font subset) |
| `/v/2009` spec line, `/v/terminal` verify and run blocks, `/v/burn` CTA and run section | SIP-1 to SIP-3 files, the repo, the clone command |

## Still at launch

| Where | Placeholder | Becomes |
| --- | --- | --- |
| `/press` "contact" | `press-contact`: "A press contact goes live at launch" | A press email or form |
| `/ecosystem` wZcash | `wz-cash`: "wz.cash (soon)", a non-link | A link to wz.cash when the site is live |
| `Base.astro` (unused today) | `<meta name="robots" content="noindex">` | Remove before a page uses it |
| All layouts | No `og:image` | Add a social card, for example from `brand/SOVA_BANNER_GOLD.jpg` |

## Where each claim comes from

| Claim on the page | Source |
| --- | --- |
| "The programmable edge of the shielded pool"; "Shielded ZEC is digital cash. Sova is what digital cash can do" | `docs/marketing/positioning.md` (one-liner, frame) |
| Landing hero and paper title: "Sova is an EVM chain that reads Zcash: contracts verify real ZEC payments on Zcash itself"; "the EVM that reads Zcash" (press one-liner) | `positioning.md` (Act II headline, Rob 2026-09-23), `docs/ROADMAP.md` ("Contracts that read Zcash (SIP-4)"), `sips/sip-4-draft-zcash-state-precompile.md` (Summary) |
| "Permissionless. Oracle-less. Self-custody."; the positive framing across pages (earned by burning ZEC, anyone can mine, every node verifies, runs on a laptop and an SSD) | Rob via the orchestrator, 2026-09-23 (no negative framing); facts as in the `/why` row below |
| Status tags: SIP-4 contracts `[testnet]` (built, in review, ships with the public testnet); mining `[local]` (regtest today); wrapped ZEC `[planned]` | Orchestrator v4 brief (honesty rules); `docs/WORKPLAN.md` row z-1 (SIP-4 v1 code complete, in CI on `z1/sip4-v1`) and Log; `docs/ROADMAP.md` (M0 box; Later: SIP-4 "ships at the testnet reset", wZEC) |
| Landing "why it exists": money gets useful when programs can react to it (a sale, a swap, a subscription, an agent paying its own way); wrapping ZEC means someone else's custody and contracts that cannot read Zcash; every Sova node runs its own Zcash node, so a contract verifies a ZEC payment directly and the ZEC stays on Zcash | Orchestrator v4 brief (draft vision copy); `positioning.md` (Act II: "Wrapped ZEC on Ethereum or Solana is an IOU whose contracts cannot read Zcash at all"); `sips/sip-4-draft-zcash-state-precompile.md` (Motivation, §5); `docs/design/ashwing-zec-checkout.md` ("The ZEC goes straight to the seller on Zcash and never leaves it") |
| Landing "what you can do": pay with ZEC from any Zcash wallet, shielded balance included, demo buy an Ashwing; ZEC↔SOVA swaps in self-custody; proof-of-burn for names, spam fees, Sybil-resistant badges; mine or let an agent mine; planned: watch Zcash's value pools from a contract (SIP-7) | `sips/sip-4-draft-zcash-state-precompile.md` §9 (use cases 1-3), `contracts/src/zcash/{ZecCheckout,ZecEscrow}.sol`, `docs/design/ashwing-zec-checkout.md`, `mcp/README.md`, `sips/sip-7-draft-zcash-events.md` (Accepted), `docs/WORKPLAN.md` row z-3 |
| Landing terminal (labelled simulated): 0.25 ZEC price + 42-zat tag = 0.25000042 ZEC, z→t from a shielded balance, 3 confirmations (testnet minConf 3), `claim(order, txid, vout)`, `txOutput` / `txInfo` checks, an Ashwing minted to the buyer. The txid, order number, token number and address are illustrative | `docs/design/ashwing-zec-checkout.md` (user story, design), `contracts/src/zcash/ZecCheckout.sol` (`claim`), `IZcash.sol`; owl image `public/ashwings/onchain-4.svg` |
| Landing "how it works", including "every Zcash answer" checked against the node's own Zcash node | `sips/sip-2.md` (Epochs, Validation), `sips/sip-3.md`, `sips/sip-4-draft-zcash-state-precompile.md` (§1 anchor; §5 answers only from the node's own index) |
| MIT or Apache-2.0 at your option; the repository is public at github.com/sova-chain/sova | `README.md` (License, clone line) |
| Paper (v5) abstract and §1: shielded ZEC moves without a ledger trail or permission; wrapped ZEC is a claim on a custodian and its contracts cannot read Zcash; every Sova node runs its own Zcash node; SOVA is issued only by burns that name an EVM address; one Sova block per Zcash block, Zcash's proof-of-work orders Sova's epochs and fixes what each one mints (not which Sova block fills an epoch); every Sova block commits to the Zcash block it settles; Sova changes nothing in Zcash and posts nothing to it | `docs/marketing/positioning.md` (frame, Act II, Terminology: PoW-anchored, "never Zcash L2"), the `/why` wrapped-ZEC row below, Zcash spec [1], `sips/sip-4-draft-zcash-state-precompile.md` (§1, §12), `sips/sip-2.md` (Epochs, Validation), `sips/sip-1.md`, `sips/sip-3.md` (Summary), `docs/ROADMAP.md` ("posts nothing to Zcash"), `docs/audits/2026-09-23-reorg-and-fork-choice.md` (§2 anchored vs not anchored; §5 Abstract correction) |
| Paper §8 Reading Zcash: the precompile at a fixed address answers from B through E_N (the anchor by height and hash; a block's hash and time; where a tx was mined, its position and depth; what a transparent output pays and to which script; whether a tx is a burn, decoded by the consensus rule); pure function of the committed chain, same state root everywhere; a node that cannot answer holds the block, never guesses; "not found" is an answer; depth = E_N − h + 1, never from a node's tip; no protocol depth, the library makes every check state one; the sale pattern (order with a fresh seller address, paid from any wallet incl. shielded, delivered at depth); a deeper Zcash reorg unwinds the delivery on every node; shielded amount, sender, recipient, memo and balance invisible; txid, height and position public even for shielded txs; a z→t output public; pre-v5 txids malleable, key on what an output pays; pool values and changes readable by a draft extension [10] | `sips/sip-4-draft-zcash-state-precompile.md` (Summary, §1-§3, §5, §7, §8, §9 use cases 1-2), `contracts/src/zcash/ZcashLib.sol` (minConf required, no default), `docs/design/ashwing-zec-checkout.md` (the pattern only; Ashwings is not named), `sips/sip-7-draft-zcash-events.md` (Summary: exact pool totals and per-tx pool flows) |
| `/press` "No peg in the protocol": ZEC stays on Zcash where contracts can see it; wrapped ZEC is a separate, custodial Sova Labs product (wz.cash), not part of the network. Paper §11: a wrapped ZEC held by a third party's signers is possible later work, would be called custody, and is no part of the design (NEAR is not named in the paper) | `docs/design/wz-cash.md`, `sips/sip-5-withdrawn.md`, `positioning.md` (Act III), `docs/WORKPLAN.md` row z-2 |
| `/build` methods, interface and status codes: `anchor`, `blockAt`, `txInfo`, `txOutput`, `burnInfo`; `spentBy` in v1.1; provisional address `0x…5a00`; display-order txids, integer zatoshis; `ZcashLib.requireOutputPays(txid, vout, script, minZat, minConf)` and `p2pkh` | `contracts/src/zcash/IZcash.sol` and `ZcashLib.sol` as on `release`; `sips/sip-4-draft-zcash-state-precompile.md` §3. Note: `z1/sip4-v1` (in review) changes `txInfo`'s `nOut` to `uint32` and adds `NOT_A_BURN = 5`; update the snippet when it lands |
| `/build` rules: deterministic answers; not found is a result, a node that cannot answer holds and retries; confirmations from the committed block, minConf required, testnet 3 / mainnet 10 (about 12.5 minutes) / more for large values; reorgs roll Sova back identically, minConf protects what happens off Sova; pre-v5 txids malleable | `sips/sip-4-draft-zcash-state-precompile.md` (§2, §3, §6, §7), `ZcashLib.sol` (NatSpec) |
| `/build` shielded stays shielded; patterns (sell for ZEC via reserve/pay/claim, `ZecCheckout.sol`; ZEC→SOVA escrow with a bond, `ZecEscrow.sol`; app burns with their own payload magic for names, badges, spam fees) | `sips/sip-4-draft-zcash-state-precompile.md` §8, §9; `contracts/src/zcash/{ZecCheckout,ZecEscrow}.sol` (NatSpec) |
| `/build#timing`: one block per Zcash block, about a minute on average, a five-minute gap normal; batch with Multicall3 (the testnet address from the deploy record) or EIP-7702 (Prague rules); pipeline nonces; `eth_sendRawTransactionSync` waits up to 300 s on a node (`SOVA_SEND_SYNC_TIMEOUT_SECS`) and 90 s through the public RPC, then poll the receipt; count time in Zcash blocks (`anchor()` height or `block.timestamp`, the Zcash block time), not `block.number`; `ZecCheckout` counts its window in Zcash blocks | `docs/design/faster-blocks.md` (§1 measurements, §4 recommendation items 3, 5, 6), `infra/testnet/deployments/sova-testnet.json` (`multicall3`), `bin/sova/src/send_sync.rs`, `infra/testnet/worker/rpc-firewall.mjs` (`SYNC_TIMEOUT_MS`), `bin/sova/src/chain.rs` (testnet = reth `DEV` hardforks, Prague at genesis), `crates/engine/src/local.rs` + `seal.rs` (`timestamp_window`: a block's time is its Zcash block's), `contracts/src/zcash/ZecCheckout.sol` (window in Zcash blocks) |
| `/build` try it locally: `forge build` / `forge test`, `npm run build` in `site/`, `npm ci` and `npm run e2e` in `tools/checkout-relayer`; needs a Chromium (`CHROME=`); e2e starts anvil, etches a mock at `0x…5a00`, deploys the checkout and drives a headless browser through reserve, pay, confirm, claim | `docs/design/ashwing-zec-checkout.md` ("Run it locally"), `tools/checkout-relayer/e2e/run.mjs` (header, `findChrome`) |
| `/build#contribute`: run a node, and "It didn't work on my machine" is a useful issue; SIP flow via Discussions then a PR to `sips/`; human + AI pairs are the default and the author explains the change; consensus code merges with `box/sim` coverage; bugs via issue templates; vulnerabilities reported privately (GitHub private vulnerability reporting or the security email) | `CONTRIBUTING.md`, `SECURITY.md`, `docs/ROADMAP.md` ("Follow and contribute") |
| `/sips` list: SIP-1 Frozen (relayed through public Zcash testnet peers, mined by an unrelated miner, txid `641cc306…`, height 4,383,754); SIP-2 Draft, implemented, proven on regtest; SIP-3 Accepted, numbers locked, schedule starts at mainnet (the public testnet mints a flat 6,250 per epoch: Rob, 2026-09-23; `sips/sip-3.md` implementation note); SIP-4 Draft, built (v1 on release), ships with the testnet; SIP-5 Withdrawn (wrapped ZEC is a custodial Sova Labs product at wz.cash, not a rule every node runs); SIP-6 Accepted and built, switches on at the testnet reset with SIP-4; SIP-7 Accepted and built (on release 2026-09-23), switches on at the testnet reset; SIP-8 Accepted, required for mainnet, on testnet after SIP-6 | `sips/sip-1.md`, `sips/sip-2.md`, `sips/sip-3.md`, `sips/sip-4-draft-zcash-state-precompile.md`, `sips/sip-6-draft-sealer-signatures.md` (status lines and summaries); `sips/sip-5-withdrawn.md`, `sips/sip-7-draft-zcash-events.md`; `docs/WORKPLAN.md` rows z-2, c-sig, z-3, audit-1 (Rob's decisions, 2026-09-23); `docs/design/wz-cash.md`; `sips/sip-8-draft-anchored-burns.md` (Status) |
| `/sips` how a SIP moves (idea in Discussions, Draft as a PR to `sips/`, Accepted = locked, Frozen = changing a rule takes a new SIP); propose one before the code | `CONTRIBUTING.md` ("SIPs govern protocol changes"), `sips/sip-1.md` (Frozen: "Changing any rule below now takes a new SIP"), `sips/sip-3.md` (Accepted: numbers locked) |
| EVM sidechain for Zcash, mined by burning ZEC; SOVA pays gas | `positioning.md`, `README.md` |
| No premine, no admin keys, no company or sequencer, no validator whitelist, no investor allocation | `positioning.md` ("Checkable neutrality", "Open participation"), `angles.md` #8, #13 |
| Burn format: 29-byte OP_RETURN, "SV", version 0x01, 20-byte EVM address, u32 signal bits; eater `76a914 00…00 88ac`; 1,000-zat floor; malformed is not a burn; weight = ZEC destroyed | `sips/sip-1.md` |
| Byte strip values (address `0x00953dbc…3523`) | `mcp/docs/walkthrough.md` (`sova_init` output), encoded per SIP-1 |
| One Zcash block = one epoch; one Sova block per Zcash block; the top burner seals, fallbacks on a ladder; pro-rata split; 1/10 tip; exact conservation; paid as block withdrawals; Zcash reorg means Sova reorg | `sips/sip-2.md`, `box/up/README.md` ("What you're seeing") |
| Burn-less epochs still get a block and mint nothing | `sips/sip-3.md` ("No mint without burns"), `sips/sip-2.md` (empty epochs), `docs/marketing/agent-keeper.md` |
| Proof of Burn, anchored to Zcash's proof of work; no puzzle | `positioning.md` (Terminology) |
| 6,250 SOVA per epoch, halving every 1,680,000 epochs (Zcash's interval), 20,000-epoch slow start like Zcash's launch; regtest mints the full reward (flat default) | `sips/sip-3.md` (Constants, rationale, implementation status) |
| Two-burner split 4,642.857142858 + 1,607.142857142 = 6,250 (also the paper's §10 worked epoch and equations (1)–(2)) | `docs/WORKPLAN.md` C3 row (ladder scenario). The 5:2 weights and the 625.000000001 tip are derived from those logged amounts using the SIP-2 math (tip = 1/10, floor pro-rata, dust to tip), and they reproduce the logged values exactly: rank 0's share floor(5,625 × 5/7) = 4,017.857142857, rank 1's 1,607.142857142 |
| Hero transcript: `init` output lines, `epoch 1: height=103 burn=100000zat fee=20000zat … txid=c5632d5e…`, baseline tip 101 | `mcp/docs/walkthrough.md`; line formats from `crates/burn-wallet/miner/src/{main,mine}.rs` |
| `INFO sova epoch trigger height=109 settled=true` | `box/up/README.md` (evidence), `crates/engine/src/driver.rs` |
| `0x152d02c7e14af680000` = 6,250 SOVA; "minted exactly 6,250 SOVA" | `box/up/README.md`, `docs/WORKPLAN.md` ("ACT I LIVE") |
| `sova-miner report --verify-rpc` diffs burns txid by txid | `crates/burn-wallet/miner/README.md`, `mcp/README.md` |
| Every node re-derives settlements from its own zebrad and rejects mismatches on every import path | `sips/sip-2.md` (Validation), `docs/WORKPLAN.md` (m1-b step 2a, SovaConsensus `efd2215`) |
| Upgrade signal bits are in the burn format from day one | `sips/sip-1.md`; wording from `angles.md` orchestrator review (C7 tally isn't built) |
| `./box/up.sh`: zebrad regtest + Sova node + miner; about 11 minutes for the first build, under 30 seconds after that; status output (136 / 106,250 SOVA / 17 epochs) | `box/README.md`, `box/up/README.md` |
| MCP server, six tools; budget-capped runs; allowance framing | `mcp/README.md`, `mcp/docs/walkthrough.md`, `agent-keeper.md` |
| Privacy at the edges: z→t funding, transparent burn, the three caveats, transparent EVM | `crates/burn-wallet/miner/README.md` ("Anonymous funding"), `positioning.md` |
| Act I / Act II, and the peg isn't live | `positioning.md` ("The two-act journey") |
| Ashwings: fully on-chain SVG, no owner, admin or metadata server; 10,000 max; one fixed price set at deploy, in SOVA or in ZEC (the ZEC checkout); "minted with destroyed money" (gas; a SOVA mint); Sova = owl; day-one mint | `contracts/src/Ashwings.sol` (NatSpec, `MAX_SUPPLY`, `priceWei`, `zecCheckout`), `contracts/src/AshwingsZecCheckout.sol`, `docs/WORKPLAN.md` G2 |
| Owl species names (classic, barn, ember for `onchain-{4,1,2}.svg`) | `contracts/src/Ashwings.sol` / `contracts/design/ashwing24.py` species, derived from each sample's seed in `contracts/samples/seeds.txt` |
| "What it isn't" list, now on `/press` (not a privacy chain, no private smart contracts, not a Zcash L2, no peg, SOVA is gas; plus "Not a PoW chain: burn-mined, ordered by Zcash's proof of work" and "No price talk"); "No peg today" (see the `/press` "No peg today" row above) | `positioning.md` ("What we never claim", Terminology), SOVA-as-gas from the brief, `docs/ROADMAP.md` ("A ZEC peg ... Not designed and not promised"; "What we won't do": no sale, no price talk) |
| MIT or Apache-2.0 | `README.md`, `LICENSE-*` |
| Paper §2 Burns: at least 1,000 zatoshis to the eater `76a914 00…00 88ac` (P2PKH, key hash of twenty zero bytes, no known preimage); exactly one 29-byte OP_RETURN output ("SV", version byte, 20-byte EVM address, 32 signal bits); weight = ZEC destroyed, summed over eater outputs; the fee is never weight; the rule is total, a malformed tx is not a burn and never an error; a burn needs nothing from Sova and is recognized after the fact; linear weight makes splitting pointless | `sips/sip-1.md` (all; Rationale "Sybil-proof by arithmetic"; the SIP-1 freeze burn mined by an unrelated miner) |
| Figure 1 (the burn transaction): one input, the eater (weight), the payload and change; the payload script `6a 1b` + "SV" + `01` + 20-byte address + 32 signal bits = 29 bytes; amounts 0.0210 in, 0.0200 eater, 0.0008 change, 0.0002 fee are illustrative | `sips/sip-1.md` (`OP_RETURN OP_PUSHBYTES_27 <payload>`, 29 bytes; 27-byte payload layout); layout per `docs/paper/v5-notes.md` §5 |
| Paper §3 Epochs and Figure 2: one Zcash block = one epoch, one Sova block per epoch at Sova height E − B + 1; blocks about every 75 s; the anchor (Zcash block hash) in the header field inherited from Ethereum's beacon chain, "Zcash is Sova's beacon"; parent hash plus anchor pin one Zcash history, so no Sova history can name a Zcash history that did not happen; the reverse does not hold: Zcash does not name Sova, several Sova histories can name one Zcash chain, and Sova chooses among them by its own rules (§5 near the tip, §10 deeper); a Zcash reorg unwinds and re-derives the Sova blocks, so a mint is final at Zcash confirmation depth; a burn-less epoch still has its block and mints nothing | `sips/sip-2.md` (Epochs, corrected 2026-09-23), `sips/sip-3.md` (Constants "at 75 s"; No mint without burns), `sips/sip-4-draft-zcash-state-precompile.md` (§1, §7; §7 notes the automatic Sova rollback is required work, the paper states the rule), `docs/audits/2026-09-23-reorg-and-fork-choice.md` (Executive summary 1 and 5, §2, §5 "Paper §3") |
| Paper §4 Issuance, equations (1)-(3): genesis allocates nothing; 9/10 of R pro rata by weight, 1/10 plus dust to the sealer, gwei floor division, exact conservation; fixed R, more burners divide it thinner (price, not supply); no carry-over; R(E) = 0.3125 · (E + 1) SOVA for E < 20,000, else 6,250 / 2^k with k = floor(E / 1,680,000); halvings floored to whole gwei, era 42 = 1 gwei, era 43 = 0, upper bound 20,937,503,124.97 SOVA; the slow start mirrors Zcash's launch; the halving interval is Zcash's; EIP-1559 base fee destroyed, priority fees and the tip to the sealer; schedule changes only by burn-weight signaling, tally rule future work | `sips/sip-2.md` (Rewards), `sips/sip-3.md` (Summary, Constants, The schedule, Why this shape, No mint without burns, rationale, Changing this schedule), `docs/ROADMAP.md` (M1 testnet chainspec; "Premine" under What we won't do; Later: burn-weight signaling needs its own SIP) |
| Paper §5 Sealing and Figure 3: rank by weight desc, ties to the byte-lexicographically smaller txid; rank 0 seals, rank r after r × step (draft step 15 s); non-burners never seal; the ladder is liveness only; rewards as the block's withdrawals (EIP-4895, Shanghai), one entry per burner in rank order, gwei; the list must match some rank's derivation; the tip makes each rank's list distinct, so the sealer's rank is read from the withdrawals; preference (rank, then hash); a late rank-0 block replaces its rank-1 sibling, one block at the tip; the same preference decides between branches (the branch rule): a block is a candidate if its ancestry, through blocks the node holds, meets the node's chain and, where it leaves it, extends the head or is preferred to the node's block there, replacing at most three of the node's blocks; candidates are compared by their blocks where their branches part; a branch that would replace more is not a candidate however ranked, so a node never replaces a block once three are built on it; the receiver decides, never the sender or arrival time, so two nodes that see the same blocks choose the same head, even after building on different blocks for up to three epochs; the block does not name its sealer yet, and the SIP-6 draft signs the header and gives an unsealed epoch one fixed empty block. Figure 3 is a schematic of that scenario (weights 5 : 2 as in §10) | `sips/sip-2.md` (Ranking and sealing, corrected 2026-09-23; Settlement, Validation roadmap), `docs/design/gossip-v1.md` ("v2 as built"; "relay delivers, arbiter decides"), `docs/WORKPLAN.md` C3 row (the ladder scenario in the nightly), `sips/sip-6-draft-sealer-signatures.md` (Summary, §1.1, §1.3, §2.4, §2.6 branch rule, §2.7), `docs/audits/2026-09-23-reorg-and-fork-choice.md` (F1 and its follow-up, §5 "Paper §5", §6.1 item 1 and its follow-up: the sibling rule was replaced by the bounded branch rule, which ships with this text), `crates/engine/src/candidates.rs` (`CandidateTracker`, `MAX_REPLACE_DEPTH = 3`) |
| Paper §6 Network: the six steps (follow the Zcash node, extract burns and settle per rank; burns are plain Zcash transactions; the sealer builds with settlement, anchor and transactions and announces by height and hash; blocks pulled, never pushed; validate against the node's own Zcash view, a valid block is a candidate if its branch meets the node's own chain as §5 requires, head by preference, re-announce only accepted blocks; build on the node's own head); `sova/1` with Announce, GetBlock and Block, no message moves a head; discovery from explicit bootnodes; own genesis and fork identifier, foreign peers refused at the handshake; catch-up by ordinary Ethereum sync, gated on the Zcash scan; between two histories that both check a catching-up node has no rule of its own yet, it takes the first tip offered and the operator should pin a recent block hash; a rule ranking whole histories and client checkpoints are future work | `docs/design/p2p-m1.md` (Decision 2 with its 2026-09-23 correction, Decision 3, "Discovery and isolation", "As built", Catch-up), `docs/design/gossip-v1.md`, `sips/sip-1.md`, `sips/sip-2.md`, `docs/WORKPLAN.md` m1-a and m1-b rows, `docs/audits/2026-09-23-reorg-and-fork-choice.md` (F2, §5 "Paper §6", §6.1 items 1 and 6, §6.2) |
| Paper §7 Verification: E_N = N + B − 1; the anchor must equal the node's own Zcash hash at E_N and the withdrawals must match some rank's settlement; withdrawals are in the body, no execution needed; enforced on the tip, short-gap and history paths; a tampered mint is rejected by a syncing node, but a history with intact mints and different transactions is another valid history (§10 says how a node chooses and what it costs to make one); a block ahead of the node's scan, or off-fork at E_N, is held; only a wrong list or state root on a matching anchor is invalid for good; a joining node scans Zcash first and syncs Sova only as far as its scan; nothing pruned; every Sova node runs a Zcash node | `sips/sip-4-draft-zcash-state-precompile.md` (§1, §6, §12), `sips/sip-2.md` (Validation roadmap), `docs/design/p2p-m1.md` (Decision 1, Decision 2, build order and AC), `crates/engine/src/consensus.rs` module docs, `docs/ROADMAP.md` (M1 built: late-join catch-up), `docs/WORKPLAN.md` m1-b row ("m1-b COMPLETE", scan-gated catch-up), `docs/audits/2026-09-23-reorg-and-fork-choice.md` (F10 mints; §2 not-anchored table; §5 "Paper §7") |
| Paper §9 Privacy: the EVM is public; privacy at the funding edge, burns and verified payments are transparent outputs that can be funded from the shielded pool; a z→t transfer has no visible input; three things stay visible (burns from one address link, the deshield is public, SOVA and its use are public); habits: fresh addresses, unremarkable amounts, time between funding and use | `crates/burn-wallet/miner/README.md` ("Anonymous funding"), `positioning.md` ("What we never claim") |
| Paper §10 Calculations: rewriting a mint needs a Zcash reorg from E_N and every node follows it identically, with the overtaking probability as in Nakamoto [6] §11; rewriting a transaction is different: any block with the epoch's settlement at some rank and the anchor is valid whatever it extends or contains, Zcash alone does not decide between two such histories and Sova adds no proof-of-work; near the tip §5's preference decides and a node replaces at most three of its blocks, only for a branch whose block where it leaves the node's chain has the better rank for the same epoch, the common case being the one block at the tip; so a transaction is settled once three more epochs are built on its block, about four minutes (3 × 75 s = 225 s); within those three blocks a rewrite costs a better-ranked block for the epoch where it begins, signed by a better-ranked burner given SIP-6, a block claiming the better rank that anyone can produce until then (audit F5); deeper an online node is never moved (a branch that would replace more than three of its blocks is not a candidate), and the same bound keeps a split deeper than three blocks from healing by itself; a joining or catching-up node takes the first valid history offered and its operator pins a recent hash until client checkpoints ship; future work named, not claimed: a preference among histories by sealer rank (rewriting from N takes every rank-0 key since N, and those sealers together can still rewrite what they sealed) and burns that name a recent Sova block (a rewrite must destroy more ZEC or reorganize Zcash); a Sova history is never harder to rewrite than the Zcash history it names; censoring k epochs means out-burning everyone k times (given SIP-6) and the transaction stays included as long as its block is not rewritten; a pro-rata SOVA costs W / ((9/10) R) in destroyed ZEC; floor burn 1,000 zat vs about 20,000 zat fee; about a quarter of a ZEC a day burning every epoch (86,400 / 75 = 1,152 epochs × 21,000 zat = 0.24 ZEC); slow start mints 62,503,125 SOVA (0.3125 × 20,000 × 20,001 / 2), 62,496,875 less than flat; eras about four years; emission ends after 43 eras, a little over 170 years (43 × 1,680,000 × 75 s = 171.7 years; SIP-3 says about 176, see `docs/paper/v5-notes.md` §4); the worked 5 : 2 epoch (row above) | `sips/sip-4-draft-zcash-state-precompile.md` (§1, §7), `sips/sip-2.md` (Epochs, Ranking and sealing, both corrected 2026-09-23), `positioning.md` (Terminology), `sips/sip-6-draft-sealer-signatures.md` (§1.1, §2.6 branch rule, §2.8, §4, corrected 2026-09-23), `crates/engine/src/candidates.rs` (`MAX_REPLACE_DEPTH = 3`), `docs/audits/2026-09-23-reorg-and-fork-choice.md` (Executive summary, §2, §4 cost table, §5 "Paper §10", F1 follow-up, §6.1 item 1 with its follow-up and checkpoints, §6.2 cumulative sealer rank, §6.3 anchored burns / SIP-8, not yet drafted), `sips/sip-3.md` (Constants, cost-floor note 20,000 to 25,000 zat), `sips/sip-1.md` (ZIP-317); derivations in `docs/paper/v5-notes.md` §3 |
| Paper §11 Conclusion: a local network (Zcash regtest node, Sova node, miner) from one command; source and specs public; the anchor and the precompile are specified and built and ship with the public testnet, the first public network; the sealer signature is built and switches on with the public testnet (SIP-6 status line); a preference on rank keeps every node's choice the same near the tip; mints are final at Zcash depth, transaction history is not yet (an online node replaces no block once three are built on it, a joining node takes the first valid history offered, the objective rules of §10 are future work); a wrapped ZEC would be custody, called custody, and is no part of this design | `docs/ROADMAP.md` (M0, Later), `box/README.md`, `docs/WORKPLAN.md` z-1 row, SIP-4 and SIP-6 status lines, `positioning.md` (Act III), `docs/audits/2026-09-23-reorg-and-fork-choice.md` (Executive summary 4 and 5, §5 "Paper §11", F1 follow-up, §7) |
| Lineage (`/story`, `/press`; cut from the paper in v5): Sova began as an EVM chain on the OP Stack with native BTC; an audit of every derivable custody address found zero BTC remaining. (The site no longer states the old chain's mainnet dates or its single key: the 2026-09-23 framing below tells the origin as a shelved plan) | `docs/marketing/comeback-thread.md` (2/11-3/11: "OP Stack chain with Bitcoin custody", every derivable address checked, zero, "Chain retired"), `docs/marketing/angles.md` #10, `docs/WORKPLAN.md` Done A1*. Dates and the single key from the audit of the old chain (`sova-review-audit` repo, findings doc: four mainnet launches 9-29 Sep 2025, the last one operating until 4 Nov 2025; custody one hot BIP32 seed; residual-BTC check in `research/btc-residual-check/`, all zero) |
| Lineage, continued (`/story`, `/press`): the chain keeps its predecessor's foundation, a reth node extended with the chain's own rules; drops the OP Stack, the bridge and custody; a user destroys ZEC and deposits nothing, so the chain holds nothing | `README.md` (layout: reth-based `bin/sova`, `crates/engine`), the old chain's `sova-reth` (a reth extension), `sips/sip-1.md` (burns go to an unspendable script); no OP Stack or custody code in this repository |
| `/why` lead (v4): Zcash is private money that works; programs react to it; contracts verify ZEC payments on Zcash itself; mined by burning ZEC, ordered by Zcash's proof-of-work. Who it's for: spend ZEC on contracts from your wallet, shielded balance included; builders verify ZEC payments; any amount mines, a laptop is enough; an agent mines its own gas. "Wrapped ZEC, or Sova": contracts read Zcash itself, self-custody. What's new [01] contracts that read Zcash (paper §6). Open by construction: every SOVA earned by burning ZEC (empty genesis), anyone can mine from 1,000 zat, every node verifies every mint, rules change through SIPs | `positioning.md` (frame, Act II), `sips/sip-4-draft-zcash-state-precompile.md`, `sips/sip-2.md`, `sips/sip-1.md` (dust floor), `docs/ROADMAP.md` (testnet genesis allocates nothing, mainnet's won't either), `CONTRIBUTING.md` (SIPs govern protocol changes), `box/README.md` (laptop), `docs/design/infra-m1.md` (zebrad row), `mcp/README.md` |
| `/story` (v4): rebuilt beside Zcash, self-custody, the ZEC stays on Zcash and contracts look at it there; "contracts that read Zcash" beat; road: testnet where contracts read Zcash (SIP-4), contracts that watch Zcash's value pools (SIP-7), mainnet | `positioning.md` (Acts II, III), `docs/ROADMAP.md` (M1, Mainnet), `sips/sip-7-draft-zcash-events.md` (Accepted) |
| `/mine` opener: mining issues SOVA, the gas that runs every contract, including the ones that verify ZEC payments. `/node`: every Sova node runs beside its own Zcash node, which is how it verifies every mint and how contracts read Zcash | `sips/sip-3.md` (SOVA minted only by epoch rewards), `positioning.md` ("SOVA is gas"), `sips/sip-4-draft-zcash-state-precompile.md` (§12: every Sova node needs a zebrad), `sips/sip-2.md` (Validation) |
| `/press` (v4): "Sova is the EVM that reads Zcash."; short description leads with contracts that read Zcash and says they ship with the public testnet; background "rebuilt beside Zcash ... self-custody"; SIP name entry | `positioning.md` (Act II headline), `sips/sip-4-draft-zcash-state-precompile.md`, `docs/ROADMAP.md` |
| `/why` who it's for: ZEC holders (burn, mine SOVA, use the EVM, no bridge); builders (Solidity and standard EVM tooling; burns are the only issuance); miners without rigs (any amount above the 1,000-zat floor mines; no hardware race, no stake); agents (a budget, then it mines on its own) | `positioning.md` ("Open participation", "Why not just Base"), `sips/sip-1.md` (dust floor), `sips/sip-3.md` (Summary: SOVA is minted only by epoch rewards to burners), `mcp/README.md`, `agent-keeper.md` |
| `/why` wrapped ZEC vs Sova: wrapped ZEC on another chain is an IOU backed by whoever holds the real ZEC (bridge, multisig, custodian) and lives under that chain's gas token, sequencer and upgrade keys; Sova holds nothing (SOVA created only by destroying ZEC), blocks follow Zcash's proof-of-work one for one, every node recomputes every mint from its own Zcash node; today only burned value crosses; contracts that read Zcash are in development (SIP-4); no peg exists | Owner direction (copy v3 brief, 2026-09-23) for the wrapped-ZEC framing; `docs/design/zec-on-sova-options.md` (custodian and multisig options for any future peg); `sips/sip-1.md`, `sips/sip-2.md` (Validation); `sips/sip-4-draft-zcash-state-precompile.md` (status: approved and in development per the orchestrator, 2026-09-23); `docs/ROADMAP.md` (no peg) |
| `/why` grassroots: no premine (the genesis allocates nothing); no sale, no treasury, no team allocation, every SOVA goes to someone who burned ZEC; no stake, mining is burning in any amount; no admin keys, no key in consensus can mint, pause or upgrade; a laptop and an external SSD run a full Zcash node and a full Sova node | `sips/sip-3.md` (Summary: all emission is epoch rewards to burners; Constants: the 1/10 sealer tip goes to the sealer, itself a burner of that epoch; "Changing this schedule": soft fork by burn-weight signaling only, no admin keys, no council), `docs/ROADMAP.md` ("What we won't do": premine, admin keys, custody, "Sell a token": no sale, no investor allocation), `positioning.md` ("Checkable neutrality"), `sips/sip-1.md` (dust floor), `docs/design/infra-m1.md` (zebrad row: Zcash mainnet state "around 300 GB", checkpoint sync measured on a laptop SSD; sova node row: a validating node runs beside its own zebrad) |
| `/why` what's new: [01] burn-mining, issuance only from destroyed ZEC split among the epoch's burners (paper §4); [02] one Sova block per Zcash block, ordered by Zcash's proof-of-work (§3); [03] every node checks every mint against its own Zcash node, synced history included (§7); [04] the sealer ladder, top burner seals, next rank after a delay, no leader election, no stake (§5); [05] `sova/1`, peers announce and serve blocks, no message moves a head (§6); [06] MCP server, six tools, hard budget (`/mine#agent`); [next] contracts that read Zcash (SIP-4, in development; paper §8) | The paper rows above; `mcp/README.md`; `sips/sip-4-draft-zcash-state-precompile.md` (status as above) |
| `/why` privacy line: the EVM is public; privacy lives at the funding edge; burn from the shielded pool and SOVA need not link to you (paper §9) | `crates/burn-wallet/miner/README.md` ("Anonymous funding"), `positioning.md` |
| `/why` fair launch (stated by negatives on purpose, the one exception to the positive framing): no premine, the genesis allocates nothing, the testnet's is empty and a test checks it, mainnet's will be too; no allocation to investors, the team or anyone; nothing in escrow, no treasury, nothing held back; the only way SOVA comes into existence is by producing blocks: burn ZEC, seal a block, earn SOVA, whoever burns and seals earns it; the local box prefunds throwaway dev accounts that never exist on a public network | Rob, 2026-09-23 (fair launch, stated plainly; "nothing held in escrow"); `sips/sip-3.md` (Summary: SOVA is minted only by epoch rewards to burners, the sealer tip to the sealer); `bin/sova/src/chain.rs` (`testnet_genesis_alloc_is_empty`: empty alloc, empty state root); `docs/ROADMAP.md` (M1 testnet chainspec: the genesis allocates nothing; "What we won't do": Premine, "mainnet's won't either", the local box's throwaway dev accounts; Sell a token: no sale, no investor allocation) |
| `/why` an experiment: an experiment and a work in progress; pre-release and unaudited; the code and the specs still change; testnet state gets reset; don't put in money you can't afford to lose | Rob, 2026-09-23; `README.md` ("Status: pre-release and unaudited"); `docs/ROADMAP.md` and `sips/sip-6-draft-sealer-signatures.md` (the testnet reset); SIP statuses (Draft, Accepted) in `sips/` |
| `/why` the plan: [first] announcement, before the testnet; [Oct 2026] public testnet, mined with testnet ZEC; [Q1 2027] mainnet, expected, when the testnet has earned it; "Dates are targets"; a line to `/story` | Rob, 2026-09-23 (the three dates); `docs/ROADMAP.md` (M1: testnet ZEC; Mainnet gates. Its "Gated, not dated" predates Rob's dates: the gates still hold, the date is a target) |
| `/story` origin (impersonal log, no signature; replaces the v4 "v1 on Bitcoin" and "the flaw" beats): Sova started as a venture-backed project focused on Bitcoin interoperability, an EVM chain on the OP Stack with native BTC; the company pivoted and went on to build other things, last year (2025) including intent-based solvers and a lot of intent-based solving around Zcash on NEAR, which is where the Zcash and NEAR connection comes from; the team decided not to go live with the original plan (Rob's exact words, chosen 2026-09-23) and shelved it; every derivable custody address of the Bitcoin-era chain was checked afterwards, zero BTC remains; Sova is back as a public-good experiment in support of the Zcash network, Zcash's programmable edge, a fun experiment, built in the open | Rob, 2026-09-23 (venture-backed, Bitcoin interoperability, the pivot, intent-based solving around Zcash on NEAR, shelved, public-good experiment for Zcash, "programmable edge", "fun experiment"); the OP Stack and native BTC and the custody check as the Lineage row above; "built in the open": the public repository (`README.md`). Deliberately no investor names, amounts or further dates. Rob's words were "not to go live with the original plan"; the page says "not to go ahead with" because the audit of the old chain records mainnet launches in September 2025 |
| `/story` "back, for Zcash": keeps the old foundation (a reth node), drops the bridge and the custody; the ZEC stays on Zcash and contracts look at it there | The Lineage, continued row above; `positioning.md` (Act II) |
| `/story` design beats: destroy ZEC and the next Sova block mints SOVA to your EVM address; one Sova block per Zcash block, ordered by Zcash's proof-of-work; every node checks every mint against its own Zcash node | `positioning.md` (frame: "shielded ZEC is digital cash"), the `/why` wrapped-ZEC row above, `sips/sip-1.md`, `sips/sip-2.md` (Epochs, Validation), `comeback-thread.md` 4/11 |
| `/story` now/next: one command runs a Zcash node, a Sova node and a miner on a laptop; nodes find each other over their own protocol, sync, and reject any history with a bad mint; the road = the announcement before the testnet, a public testnet in October 2026 mined with testnet ZEC where contracts read Zcash (SIP-4), contracts that watch Zcash's value pools (SIP-7), mainnet expected Q1 2027 when the testnet has earned it; "Dates are targets" | `box/README.md`, the paper §6 Network and §7 Verification rows above; the dates: Rob, 2026-09-23 (as the `/why` plan row below); `docs/ROADMAP.md` (M1, Mainnet gates; its "Gated, not dated" line predates Rob's dates), `sips/sip-4-draft-zcash-state-precompile.md`, `sips/sip-7-draft-zcash-events.md` |
| `/press` background paragraph (venture-backed, Bitcoin interoperability, an EVM chain on the OP Stack with native BTC; the company pivoted, including intent-based solving around Zcash on NEAR; the original plan shelved; zero BTC on every derivable custody address; back as a public-good experiment in support of Zcash, mined by burning ZEC, the ZEC stays on Zcash) | As the `/story` origin and Lineage rows above (Rob, 2026-09-23) |
| `/press` community; footer "Telegram" and "X"; repo `README.md` "Community": <https://t.me/sovazec>, <https://x.com/sovazec> | Rob, 2026-09-23 |
| `/mine`: `init` / `mine` / `report --verify-rpc` commands and flags; init output labels and values; the epoch output line; `MATCH: yes`; budgets (`--budget-zat` per run incl. fee, `--lifetime-budget-zat`, `--max-epochs`); fee about 20,000 zat per burn, never burn weight; `--network test` and `--rpc-cookie-file` | `crates/burn-wallet/miner/README.md`, `crates/burn-wallet/miner/src/main.rs` (clap flags, `init` output), `crates/burn-wallet/miner/src/mine.rs` (epoch line format), `mcp/docs/walkthrough.md` (addresses, epoch 1 values, change, `MATCH: yes`), `sips/sip-1.md` (ZIP-317 fee never counts toward weight), `sips/sip-3.md` (cost floor note) |
| `/mine` agent path: `claude mcp add sova-miner -- node "$(pwd)/mcp/dist/index.js"`, the example prompt, the six tools | `mcp/README.md` (Tools, walkthrough steps 2-3) |
| `/mine` privacy: z→t funding; burns from one address link, deshield visible, payouts public; fresh miner per session, avoid round amounts | `crates/burn-wallet/miner/README.md` ("Anonymous funding") |
| `/node`: prerequisites (Docker, `zfnd/zebra:6.3.0` about 120 MB, Rust, curl, python3, 2.3 GB target + 1.7 GB caches, ports 18232/8545/8551/30303); about 11 minutes first build; under a minute after; status output; `settled=true` log lines at 109 and 111; every-other-block burns; 6,250 SOVA hex; three components; `deploy-dapps.sh` kit; teardown; troubleshooting | `box/README.md`, `box/up/README.md` (Prerequisites, quickstart, "What you're seeing", day-one dapp demo, Troubleshooting, Evidence) |
| `/node` testnet: the public testnet will run the same node with its own genesis and pinned bootnodes | `docs/WORKPLAN.md` m1-a row, `docs/design/p2p-m1.md` ("As built") |
| `/ashwings`: SVG and metadata built by the contract from a seed fixed at mint; no owner, admin or metadata server; 10,000 max, one fixed price in SOVA or ZEC; `ASHW`, "Ashwing #N"; "minted with destroyed money"; 24 × 24 grid, left half mirrored, pupils placed after mirroring; eleven species with weights 51/31/31/26/26/20/20/20/15/11/5 out of 256 (shown rounded to percent: 20/12/12/10/10/8/8/8/6/4/2) and body colours; eyes, iris, chest, tufts, beak, background, accessory values; day-one mint | `contracts/src/Ashwings.sol` (NatSpec, `mint`, `_traits`, `SPECIES_W`, `SPECIES_RGB`, `_attributes`, `tokenURI`), `contracts/design/ashwing24.py`, `docs/WORKPLAN.md` G2 |
| `/ashwings` gallery labels (species of each sample) | `contracts/samples/seeds.txt` line N is the seed of `onchain-N.svg` (rendered by `contracts/script/Samples.s.sol`); species from the contract's `traitsOf` / `ashwing24.py --seed`: 0 snowy, 1 barn, 2 ember, 3 bronze, 4 classic, 5 spectral, 6 dusk, 7 midnight, 8 moss, 9 classic, 10 slate, 11 bright |
| `/press`: primary and secondary one-liners; short description; name usage (Sova, SOVA as gas only, Ashwings / an Ashwing, burn-mined / Proof of Burn, PoW-anchored; never PoW chain, Zcash L2, privacy chain, private smart contracts; no price framing; no peg claims) | `positioning.md` (one-liner, naming note, "What we never claim", Terminology), `contracts/src/Ashwings.sol` |
| `/press` logos and palette (Gold `#F4B728`, Deep gold `#B0841D`, Deep gold 600 `#967019`, Gold 50 `#FBF3DC`, warm black `#1C1914`); use rules (don't recolour, stretch, add effects; gold on dark, deep gold on light) | `brand/readme.md` (ZEC-era recolor note, logo variations, palette, usage guidelines); files copied from `brand/` |
| `/ecosystem`: the network (the chain every node runs, verifies every mint against its own Zcash node, rules in the SIPs; permissionless, oracle-less, self-custody); apps are run by their builders and the network runs every contract by the same rules | `sips/sip-2.md` (Validation), `CONTRIBUTING.md` (SIPs govern protocol changes), the "Permissionless. Oracle-less. Self-custody." row above |
| `/ecosystem` Ashwings: 10,000 owls drawn by their own contract, mint with SOVA on Sova or ZEC on Zcash, at launch; a mint page and a marketplace are being built | `contracts/src/Ashwings.sol`, `docs/WORKPLAN.md` G2 and the 2026-09-23 log (Rob: 10,000 cap, fixed price in SOVA or ZEC, 1% market fee); the mint and marketplace pages: orchestrator brief, 2026-09-23 (another branch builds them under `/ashwings`) |
| `/ecosystem` Zcash pulse: shielded pool totals read live from Sova state every block (SIP-7); a demo that runs against a local node today; tag `[testnet]` | `src/pages/pulse.astro`, `tools/pulse-feed/e2e.mjs`, `sips/sip-7-draft-zcash-events.md` (Accepted; implementation pending, targets the testnet reset) |
| `/ecosystem` day-one kit: WSOVA, a Uniswap-v2-class exchange (factory and router) and Multicall3; launch a token, pool it and swap it from the first block; settings fixed at deploy (the factory's `feeToSetter` is the zero address); one command on the box, `./box/deploy-dapps.sh`; tag `[local]` | `box/deploy-dapps.sh` (header), `contracts/script/Deploy.s.sol` (NatSpec: "No owners anywhere"), `box/up/README.md` ("Optional: the day-one dapp demo"), `docs/WORKPLAN.md` G1 |
| `/ecosystem` wZcash: "custodial, by Sova Labs", a Sova Labs product at wz.cash (soon), apart from the network; the ZEC sits in a Zcash address whose key NEAR's MPC network holds (Chain Signatures); a Sova Labs relayer tells the vault about withdrawals on Sova; a small fee on wraps and unwraps; holding it means trusting Sova Labs and NEAR's MPC operators; ZEC in your own wallet: pay from Zcash directly (SIP-4) | `docs/design/wz-cash.md` (Rob's decisions 2026-09-23: product, relayer, fee; "Custody, stated first"), `sips/sip-5-withdrawn.md`, `sips/sip-4-draft-zcash-state-precompile.md` |
| `/ecosystem` build yours: contracts read Zcash (SIP-4) and its shielded pool totals (SIP-7), tag `[testnet]`; list a project by a pull request to this page | `sips/sip-4-draft-zcash-state-precompile.md`, `sips/sip-7-draft-zcash-events.md` (status lines); the page's own path in this repository |
| Homepage directions, shared: "Permissionless. Oracle-less. Self-custody."; "An EVM sidechain, one block per Zcash block"; doors (Mine: `sova-miner mine`, burn ZEC, earn SOVA; Run a node: `./box/up.sh`, Zcash and Sova side by side on a laptop; Build: `IZcash.txInfo()`, contracts that read Zcash); status "pre-release · public testnet live" | The positive-framing row above; `sips/sip-2.md` (Epochs); `crates/burn-wallet/miner/README.md`; `box/README.md` (laptop), `docs/design/infra-m1.md` (laptop SSD); `contracts/src/zcash/IZcash.sol`; `docs/ROADMAP.md` (M0 local, M1 public testnet) |
| `/v/block` headline "Burn ZEC. Mine SOVA. On your laptop."; terminal (labelled regtest, shortened): `./box/up.sh` brings up zebrad, a Sova node and the miner; `sova-miner mine --per-epoch-zat 100000` (the miner's network defaults to regtest); Zcash block 103 found; `epoch 1: height=103 burn=100000zat …`; burned to `76a914 00…00 88ac`, unspendable; `INFO sova epoch trigger height=103 settled=true`; +6,250 SOVA to `0x0095…3523`. The chain strip: Zcash heights 103-112, burns at the odd heights, one Sova block each, +6,250 where there was a burn and +0 where there was none | `mcp/docs/walkthrough.md` (epoch 1 at height 103, 100000 zat, the five burns at 103/105/107/109/111, the EVM address), `crates/burn-wallet/miner/src/main.rs` (`--network` default `regtest`), `box/README.md`, `box/up/README.md` (every-other-block burns, `settled=true` line format), `sips/sip-1.md` (eater), `sips/sip-2.md` (one block per epoch, burn-less epochs), `sips/sip-3.md` (6,250; regtest mints the full reward), `src/data/sova.ts` `transcript` (the same values already on the site) |
| `/v/fire` headline "Every SOVA began as burned ZEC."; ZEC → `76a914 00…00 88ac` → SOVA; steps: burn ZEC on Zcash, any amount from 1,000 zatoshis; Sova seals the matching block, one per Zcash block; SOVA mints to the address the burn names. The coin animation is an illustration | `sips/sip-3.md` (SOVA minted only by epoch rewards to burners), `docs/ROADMAP.md` (the testnet genesis allocates nothing and mainnet's won't either; the claim is about the public networks, the local box's dev chainspec prefunds reth's dev accounts), `sips/sip-1.md` (eater, dust floor, EVM address in the payload), `sips/sip-2.md` (Epochs, Ranking and sealing) |
| `/v/cover` headline "The EVM that reads Zcash."; abstract: every node runs a Zcash node, so a contract can check a ZEC payment against Zcash itself; SOVA is issued only by burning ZEC, one Sova block per Zcash block; Figure 1: one Sova block per Zcash block, each commits to the Zcash block it settles | `positioning.md` (Act II headline), `sips/sip-4-draft-zcash-state-precompile.md` (§1, §12), `sips/sip-3.md`, `sips/sip-2.md` (Epochs), paper §3 (the row above) |
