---
// /paper: the whitepaper (Rob's pick, 2026-09-22), moved here from / when the
// landing page took the root (site v4). The page lives in
// components/Paper.astro so /v/paper keeps rendering it among the directions.
// Paper v5 (site/src/components/Paper.astro): section anchors are names
// (#intro … #conclusion, #refs); the v4 ids (#s1 … #s10, #s-zcash, #fig4, …)
// are kept as empty spans at the equivalent place so old links still land.
import Paper from '../components/Paper.astro';
---
<Paper home />
